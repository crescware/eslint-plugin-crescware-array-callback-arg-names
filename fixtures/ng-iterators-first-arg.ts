declare const arr: number[];

arr.map((e) => e);
arr.filter((e) => e > 0);
arr.forEach((e) => {
  void e;
});
arr.find((e) => e > 0);
arr.findIndex((e) => e > 0);
arr.findLast((e) => e > 0);
arr.findLastIndex((e) => e > 0);
arr.some((e) => e > 0);
arr.every((e) => e > 0);
arr.flatMap((e) => [e]);
